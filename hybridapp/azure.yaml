# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: copilot-agentops

metadata:
  template: azd-init@1.14.0

services:
  copilot-agentops:
    project: src/CopilotAgentOpsMcp.HybridApp
    host: containerapp
    language: dotnet
    docker:
      path: ../../../Dockerfile.copilot-agentops-azure
      context: ../../../
      remoteBuild: true
